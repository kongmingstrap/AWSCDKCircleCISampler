AWSCDKCircleCISampler
===

```shell
brew install anyenv
```

```
export PATH="$HOME/.anyenv/bin:$PATH"
eval "$(anyenv init -)"
ANYENV="$HOME/.anyenv/bin"
PATH=$PATH:$HOME/.local/bin:$HOME/bin:$ANYENV
export PATH

eval "$(anyenv init -)"
```

```shell
$ exec $SHELL -l
```
